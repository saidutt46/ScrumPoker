import { EnumArrayPipe } from './enum-array.pipe';

describe('EnumArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
